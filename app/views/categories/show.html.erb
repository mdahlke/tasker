<p id="notice"><%= notice %></p>

<p>
<h1><%= @category.name %></h1>
<p><%= @category.id %></p>
</p>

<% @tasks.each do |task| %>
<%= render 'tasks/tasks' %>
<% end %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
