<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<p>
  <strong>Category:</strong>
  <%= @task.category.name %>
</p>

<span class="btn-priamry"><%= link_to 'Edit', edit_task_path(@task), class: 'btn-a btn-success' %></span> |
<%= link_to 'Back', tasks_path, class: 'btn-primary btn-a' %>
